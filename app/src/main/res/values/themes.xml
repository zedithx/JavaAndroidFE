<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JavaAndroidApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.JavaAndroidApp" parent="Base.Theme.JavaAndroidApp" />

    <!--Switch Toggle Button-->
    <style name="red_switch_theme" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/lightred</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/lightgray
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/darkgray
        </item>
    </style>

</resources>